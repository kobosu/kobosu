 <p><%= flash[:notice] %></p>
 
<% if @shipping_address.errors.any? %>
  <%= @shipping_address.errors.count %>error prohibited this obj from being saved:
  <ul>
    <% @shipping_address.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h2>配送先編集</h2>
<%= form_with model: @shipping_address do |f| %>
<div class="form-group">
  <p for="shipping_address_postcode">郵便番号(ハイフンなし)</p>
  <%= f.text_field :postcode %>
  </div>
  
  <div class="form-group">
  <p for="shipping_address_address">住所</p>
  <%= f.text_area :address %>
  </div>
  
    <div class="form-group">
  <p for="shipping_address_name">宛名</p>
  <%= f.text_area :name %>
  </div>
  
   <div class="form-group">
  <%= f.submit '変更内容を保存', class: "btn btn-success" %>
  </div>
<% end %>